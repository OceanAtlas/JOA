
package ndEdit;


/** @note Allows the user to type ranges in via text fields. 
  * For CENTER_WIDTH,  "Center" and "Width" are column headers.  
  * Row headers are lat, lon, depth, time.  
  * For MIN_MAX, "Min", "Max", are column headers.  Row headers are 
  * same as above.
  */
public class LiteralRangeEntryForm {
}
